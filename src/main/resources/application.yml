server:
  port: 80

mybatis:
  mapper-locations: classpath:mappers/**/**.xml

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/mydb?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: lgs0503
    password: 0609